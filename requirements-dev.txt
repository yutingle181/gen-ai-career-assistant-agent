# 开发 / CI 测试依赖（不装 torch，保持 CPU 轻量）
# 安装：pip install -r requirements-dev.txt
# 继承生产依赖：编译出的 requirements-dev.lock.txt 会包含全部主依赖，CI 装这一个即可。
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0      # 覆盖率统计（配置见 pyproject.toml）
httpx>=0.27          # starlette TestClient（test_api.py）依赖
ruff>=0.16,<0.17     # 静态检查 / 导入排序（配置见 pyproject.toml）
